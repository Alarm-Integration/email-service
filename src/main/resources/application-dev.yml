server:
  port: 30030

spring:
  application:
    name: email-service

eureka:
  instance:
    prefer-ip-address: true
    ip-address: "${HOST_IP}"
    non-secure-port: 8080

springfox:
  documentation:
    swagger:
      v2:
        path: /user-service/v2/api-docs